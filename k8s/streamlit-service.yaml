apiVersion: v1
kind: Service
metadata:
  name: streamlit
  namespace: extraquiz
spec:
  type: LoadBalancer
  selector:
    app: streamlit
  ports:
    - name: http
      port: 80
      targetPort: 8080
